import styles from './userPage.module.scss';

export const UserPage = () => {
  return (
    <div className={`${styles.user} wrapper`}>
      <div className="register__container">UserPage</div>
    </div>
  );
};
